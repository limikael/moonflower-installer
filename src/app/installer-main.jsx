#!/usr/bin/env -S wun -h450 -w750 -t "Install Moonflower"

import InstallerApp from "./InstallerApp.jsx";

let el=document.createElement("div");
document.body.appendChild(el);

render(<InstallerApp/>,el);
